export const metadata = {
  title: "Locations | Hertz Thailand",
  description: "Find Hertz rental locations at airports and cities across Thailand",
};

export default function LocationsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
