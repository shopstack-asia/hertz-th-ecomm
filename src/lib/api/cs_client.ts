/**
 * Commerce Suite (CS) client wrapper
 * Phase 1: Stub - no actual CS calls. Swap with real implementation later.
 */

// Future: import { csApi } from './cs_api';

export const csClient = {
  // locations: async (params) => csApi.getLocations(params),
  // search: async (params) => csApi.searchVehicles(params),
  // vehicle: async (groupCode) => csApi.getVehicle(groupCode),
  // pricing: { validate: async (params) => csApi.validatePricing(params) },
  // reservation: { create: async (params) => csApi.createReservation(params) },
  // payment: { initiate: async (r) => csApi.initiatePayment(r), callback: async (p) => csApi.paymentCallback(p) },
  // booking: { get: async (no) => csApi.getBooking(no) },
  // account: { profile: {...}, bookings: {...} },
} as const;
